# Application

This directory is reserved for the interactive ThermoBreastAI application.

The public application should present model outputs clearly while keeping the project framed as an academic research prototype rather than a clinical diagnostic tool.

Planned entry point:

- `app.py` — Streamlit interface
