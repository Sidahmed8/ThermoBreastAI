# Source Code

This directory is reserved for the reusable Python modules used by ThermoBreastAI.

Planned organization:

- `preprocessing.py` — thermal-image preprocessing
- `train.py` — model training
- `evaluate.py` — model evaluation
- `predict.py` — inference utilities

Only code that reflects the actual project implementation should be added here.
